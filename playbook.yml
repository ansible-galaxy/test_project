---
- hosts: localhost
  gather_facts: true
  tasks:
    - name: Check version of hub.foobar
      ansible.builtin.debug:
        msg: "hub.foobar version {{ lookup('community.general.collection_version', 'hub.foobar') }}"
